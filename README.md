# FileStorage
A simple file storage website made in Flask

## How to run
1. Download the source code
2. Open the command line, change directory into FileStorage's directory and run `docker build -t filestorage .` command. You can skip this step if filestorage's image is already built
3. Run `docker run -p 5000:5000 FileStorage`
# FileStorage
A simple file storage web application made in Flask

## How to run
1. Download the source code
2. Open the command line and change directory into FileStorage's directory
3. Run `docker-compose up` command. If you don't have an image build, use `docker-compose up --build` command